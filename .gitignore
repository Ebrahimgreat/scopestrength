# The directory Mix will write compiled artifacts to.
/_build/

# If you run "mix test --cover", coverage assets end up here.
/cover/

# The directory Mix downloads your dependencies sources to.
/deps/

# Where 3rd-party dependencies like ExDoc output generated docs.
/doc/

# Ignore .fetch files in case you like to edit your project deps locally.
/.fetch

# If the VM crashes, it generates a dump, let's ignore it too.
erl_crash.dump

# Also ignore archive artifacts (built via "mix archive.build").
*.ez

# Temporary files, for example, from tests.
/tmp/

# Ignore package tarball (built via "mix hex.build").
scopestrength-*.tar

# Ignore assets that are produced by build tools.
/priv/static/assets/

# Ignore digested assets cache.
/priv/static/cache_manifest.json

# In case you use Node.js/npm, you want to ignore these.
npm-debug.log
/assets/node_modules/

.env

# User-uploaded files live on the server and must never be committed.
# This was previously written as ".env/priv/static/uploads/*", which git read
# as a single path and silently ignored nothing.
/priv/static/uploads/

# Digested copies produced by phx.digest. The hash suffix is 32 hex chars, so
# match that exactly — a looser "*-*" would also catch source names that
# legitimately contain hyphens, like barlow-condensed-700.woff2.
/priv/static/**/*-[0-9a-f][0-9a-f][0-9a-f][0-9a-f][0-9a-f][0-9a-f][0-9a-f][0-9a-f][0-9a-f][0-9a-f][0-9a-f][0-9a-f][0-9a-f][0-9a-f][0-9a-f][0-9a-f][0-9a-f][0-9a-f][0-9a-f][0-9a-f][0-9a-f][0-9a-f][0-9a-f][0-9a-f][0-9a-f][0-9a-f][0-9a-f][0-9a-f][0-9a-f][0-9a-f][0-9a-f][0-9a-f].*
/priv/static/**/*.gz

# Database dumps -- these contain real user data, never commit them.
# Scripts we write ourselves (priv/repo/*.sql) are intentionally NOT covered
# here -- only actual pg_dump/pg_restore output, which should never live
# inside the repo tree in the first place.
*.dump
/*.sql

.DS_Store

# Written by the programme download; regenerated on every request.
programme.txt
